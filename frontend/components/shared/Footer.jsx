const Footer = () => {
    return (
        <footer className="fixed bottom-0 left-0 right-0 flex justify-center py-4 text-xs">
            TipItSimple {new Date().getFullYear()}
        </footer>
    )
}

export default Footer